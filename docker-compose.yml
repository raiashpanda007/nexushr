version: "3.9"

services:
  nginx:
    image: nginx:trixie-perl
    ports:
      - "5000:80"
    volumes:
      - ./client:/var/www/app
      - ./conf/nginx.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
